import { AppRegistry } from 'react-native';
import App from './App';

// Register the app with the 'main' entry point for native platforms
AppRegistry.registerComponent('main', () => App);

console.log('Native entry point loaded'); 