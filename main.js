import { AppRegistry } from 'react-native';
import App from './App';

// Register the app with the 'main' name explicitly
AppRegistry.registerComponent('main', () => App);

// Export the App component as default
export default App; 